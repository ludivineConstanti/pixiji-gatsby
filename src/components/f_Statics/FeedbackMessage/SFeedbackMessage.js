import styled from "styled-components"

import { tFeedback } from "src/styles/typo"

export default styled.div`
  ${tFeedback}
  margin-top: 16px;
  margin-bottom: 4px;
  ul {
    list-style: inside;
  }
`
